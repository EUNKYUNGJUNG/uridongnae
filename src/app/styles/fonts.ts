import { Open_Sans } from "next/font/google";
import { Nanum_Gothic } from "next/font/google";

export const sans = Open_Sans({ subsets: ["latin"] });
export const thinGothic = Nanum_Gothic({
  weight: "400",
  subsets: ["latin"],
});
export const gothic = Nanum_Gothic({
  weight: "700",
  subsets: ["latin"],
});
